<h1><%= @post.title %></h1>
<p><%= @post.content %></p>
<td><%= link_to 'Edit', edit_post_path(@post) %></td>
<td><%= button_to 'Delete', post_path(@post), method: :delete, data: {confirm: "Are you sure?"} %></td>
